
// This file is now empty as we're removing all mock data
import { CameraResult } from '@/types/scanner';

// Creating an empty array instead of mock data
export const mockCameras: CameraResult[] = [];
